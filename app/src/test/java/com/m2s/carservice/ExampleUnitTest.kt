package com.m2s.carservice

import org.junit.Test

import org.junit.Assert.*

/**
 * m2s local unit test, which will execute on the development machine (host).
 *
 * See [testing documentation](http://d.android.com/tools/testing).
 */
class m2sUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}